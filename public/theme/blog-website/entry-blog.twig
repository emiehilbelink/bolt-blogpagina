{% extends 'partials/_master.twig' %}

{% block main %}
    <div class="record-title">
        <h1>{{ record.title }}</h1>
    </div>

    <div class="blog-img">
            <img src="{{ image(record.image) }}">
     </div>

    <div class="record-body">
      <p>  {{ record.body}} </p>
    </div>

{% endblock main %}
